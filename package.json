{
  "name": "toile-collaborative",
  "version": "1.0.0",
  "description": "Grande toile collaborative persistante avec rôles passager et admin.",
  "private": true,
  "scripts": {
    "start": "php -S 0.0.0.0:4173 router.php",
    "check": "node --check src/main.js && php -l router.php"
  }
}
